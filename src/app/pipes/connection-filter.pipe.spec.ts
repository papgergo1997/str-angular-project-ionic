import { ConnectionFilterPipe } from './connection-filter.pipe';

describe('ConnectionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
