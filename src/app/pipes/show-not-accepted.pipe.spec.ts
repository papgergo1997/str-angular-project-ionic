import { ShowNotAcceptedPipe } from './show-not-accepted.pipe';

describe('ShowNotAcceptedPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNotAcceptedPipe();
    expect(pipe).toBeTruthy();
  });
});
