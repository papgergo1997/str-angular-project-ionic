import { ShowAcceptedConnectionsPipe } from './show-accepted-connections.pipe';

describe('ShowAcceptedConnectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowAcceptedConnectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
